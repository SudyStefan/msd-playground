### Snake Game
- made to get familiar with frameworkless JS/TS

### run locally
- requires node and npm
- in root dir:
  - init: `npm install`
  - run dev: `npm run dev`
  - build for deployment: `npm run build`
    - output in dist/